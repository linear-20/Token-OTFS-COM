"""Experiments package -- standalone experiment harnesses.

These modules are not part of the transmitter package public API.
They consume transmitter APIs but do not expose new transmitter symbols.
"""
